# Firebase App Hosting configuration
# https://firebase.google.com/docs/app-hosting/configure
runConfig:
  minInstances: 0
  maxInstances: 2
  concurrency: 80
  cpu: 1
  memoryMiB: 512

# Environment variables and secrets available to the running server.
# The public site URL is used for canonical tags, sitemap and JSON-LD.
env:
  - variable: NEXT_PUBLIC_SITE_URL
    value: https://goldendiamondupholstery.co.za
    availability:
      - BUILD
      - RUNTIME

  # --- Contact form email (Resend) ---
  # The quote form emails each lead to CONTACT_TO when these are set.
  # 1) Create a Resend account + API key (https://resend.com).
  # 2) Store the key in Cloud Secret Manager, then reference it here:
  # - variable: RESEND_API_KEY
  #   secret: RESEND_API_KEY        # name of the secret in Secret Manager
  #   availability: [RUNTIME]
  # Google Analytics 4 measurement ID (looks like G-XXXXXXXXXX). Create the
  # property at analytics.google.com, choose a Web data stream for
  # https://goldendiamondupholstery.co.za, then paste the ID here and redeploy.
  # Analytics stays completely inert while this is blank. BUILD availability is
  # required: NEXT_PUBLIC_* values are inlined into the bundle at build time.
  - variable: NEXT_PUBLIC_GA_ID
    value: G-FK0WGZ1FG9
    availability:
      - BUILD
      - RUNTIME

  # Where quote requests are emailed. Accepts a comma-separated list, e.g.
  #   value: sales@goldendiamondupholstery.co.za,info@goldendiamondupholstery.co.za
  # NOTE: this address must be on a domain with working MX records — see README.
  - variable: CONTACT_TO
    value: hello@goldendiamondupholstery.co.za
    availability: [RUNTIME]
  # Once your domain is verified in Resend, set a branded from-address:
  # - variable: CONTACT_FROM
  #   value: Golden Diamond Website <quotes@goldendiamondupholstery.co.za>
  #   availability: [RUNTIME]
